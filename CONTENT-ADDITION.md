# 作品の追加・差し替えガイド

このサイトは **「`works/` フォルダが唯一の正（source of truth）」** になるように作ってあります。
あなたは `works/` にファイルを置くだけ。`index.html` の更新は **Claude Code に一声かければOK**（手編集も可）。

更新頻度が低い前提なので、自動ビルドの仕組みは入れていません。

---

## フォルダの形（これが全て）

```
works/
  <カテゴリ名>/              ← フォルダ名がそのまま見出しになる（日本語OK・半角スペース禁止）
    _label.txt              ← 任意。見出しの表示名を別にしたい時だけ置く（例: スペース入り表記）
    <NNN-slug>/             ← 1作品。NNN=3桁通し番号（並び順）、slug=半角英小文字とハイフン
      caption.txt           ← 【必須】1行目＝画面に出すキャプション
      （以下、在るものだけ置く）
      video.mp4             ← 動画本体（H.264）
      video.webm            ← 任意（VP9。あれば優先配信）
      poster.jpg            ← 動画があるなら必須（静止画／reduced-motion時の表示）
      image.jpg             ← 画像1枚のとき
      image-1.jpg image-2.jpg …   ← 画像が複数のとき（連番）
      image.webp / image-1.webp … ← 任意（あれば優先配信）
```

### ルール
- **1作品 = 1フォルダ**。中に動画・画像を何個入れてもよい（動画のみ／画像のみ／動画+画像/複数画像、何でも可）。
- **カテゴリ = 親フォルダ**。新カテゴリは新しいフォルダを作るだけ。
- **フォルダ名に半角スペースは使わない**（日本語はOK）。見出しをスペース入り等で表示したい場合のみ、カテゴリ直下に `_label.txt` を置き、1行目に表示名を書く。無ければフォルダ名がそのまま見出しになる。
- **キャプション = `caption.txt` の1行目**。（2行目以降は今は未使用。将来の説明文用に予約）
- **メディアの表示順** = 動画 → 画像（`image-1`→`image-2`…のファイル名順）。
- 動画と画像を同じ作品に入れるなら、同じフォルダに両方のファイルを置く。

---

## 作業手順

### A. 既存作品の差し替え（動画・画像・キャプション）
`works/<カテゴリ>/<作品>/` の中の **同名ファイルを上書き**するだけ。
- 動画 → `video.mp4`（+ `video.webm` / `poster.jpg`）を上書き
- 画像 → `image.jpg`（+ `image.webp`、複数なら `image-1.jpg` …）を上書き
- キャプション → `caption.txt` の1行目を書き換え

> 動画・画像の**ファイルだけ**の差し替えなら、`index.html` は変わらないので **Claude Code 不要**。
> キャプション（`caption.txt`）を変えた場合は、`index.html` にも反映が要るので下の「依頼」を実行。

### B. 作品を追加する
1. `works/<カテゴリ>/<NNN-slug>/` フォルダを作る。
2. メディアファイルと `caption.txt` を置く（上の「フォルダの形」参照）。
3. 下の「Claude Code への依頼」を実行 → `index.html` に反映してもらう。

### C. カテゴリを追加・改名・並べ替え
- 追加: `works/` に新しいカテゴリフォルダを作り、中に作品フォルダを入れる。
- 改名: カテゴリフォルダ名を変える。
- 並べ替え: 「依頼」時に希望の順番を伝える（順番はフォルダ名からは決まらないため）。

---

## Claude Code への依頼（コピペ用）

`works/` を更新したあと、Claude Code にこう頼んでください：

```
works/ の現在の内容に合わせて index.html を再生成して。
ルールは CONTENT-ADDITION.md に従う。
（カテゴリの並び順は ゲーム作品 → VRChat ワールド制作 → その他 の順で）
```

Claude 側は次を機械的に行います：
- `works/` を走査し、カテゴリ（親フォルダ）ごとに `<section>` を作る
- 見出しは `_label.txt` があればその1行目、無ければフォルダ名を使う
- 各作品フォルダの **在るファイルから** 動画/画像ブロックを組み立てる
- `caption.txt` の1行目を `figcaption` と各メディアの `aria-label`／`alt` に反映
- パスはフォルダ名（日本語可）をそのまま使う（半角スペースは使わない運用なので `%20` は発生しない）

---

## 注意点
- カテゴリフォルダ名に**半角スペースは使わない**。スペース入り等の見出しにしたい時は `_label.txt` を使う（例: フォルダ `VRChatワールド制作` + `_label.txt`=「VRChat ワールド制作」）。
- 並び順だけはフォルダ名から自動で決まらないので、希望があれば依頼時に伝える（既定は現状の順を維持）。
- 動画は H.264 mp4 推奨。重い動画に差し替えたら、表示速度のため解像度/ビットレートを下げる検討を。
